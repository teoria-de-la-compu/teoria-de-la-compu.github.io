#include <assert.h>
#include "palindromo.h"

int main(void) {
    /* para cada fila de tabla de casos */
    assert(es_palindromo("aba") == 1);
    /*assert(es_palindromo("abba") == 1);*/

    return 0;
}

